# -*- coding: utf-8 -*-
# ──────────────────────────────────────────────────────────────────────────────
# rvg_remote.py — کانفیگ همگام‌سازی state پنل با یک secret gist روی GitHub
#
# چرا این فایل؟ چون فقط فایل‌های .py از فیلترهای بعضی دیپلوی‌ها جان سالم به در
# می‌برند، توکن و تنظیمات remote باید داخل یک فایل پایتونی باشد.
#
# کاری که می‌کند: کل state پنل (رمز ادمین، نشست‌ها، کانفیگ‌ها، ساب‌ها) به‌صورت
# یک secret gist روی GitHub ذخیره می‌شود تا اگر کانتینر/سرور ری‌استارت شد یا
# دیسکش موقتی بود، پنل دقیقاً با آخرین وضعیت بالا بیاید (رمز عوض نشود،
# کانفیگ‌ها پاک نشوند، لازم نباشد دوباره لاگین کنید).
#
# فعال‌سازی: یک GitHub Classic Token با دسترسی «gist» بسازید ( Settings →
# Developer settings → Personal access tokens → Tokens (classic) ) و جای خالی
# زیر بگذارید. هیچ چیز دیگری لازم نیست؛ gist خودکار ساخته/پیدا می‌شود.
# ──────────────────────────────────────────────────────────────────────────────

GITHUB_TOKEN = ""          # ← توکن classic با اسکوپ gist (خالی = غیرفعال)
GIST_ID = ""               # معمولاً خالی بگذارید؛ gist خودکار پیدا/ساخته می‌شود
ENABLED = True             # False = همگام‌سازی remote کلاً خاموش
PUSH_DEBOUNCE_SECONDS = 15 # حداقل فاصله بین دو push پشت‌سرهم به GitHub
